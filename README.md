# Flask WebSocket Backend

This project implements a WebSocket backend using Flask and Flask-SocketIO. It allows users to subscribe, send messages, and receive real-time streaming responses from the OpenAI Assistant API.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt